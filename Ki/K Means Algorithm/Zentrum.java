import java.util.ArrayList;
import java.util.TreeMap;

public class Zentrum {

    ArrayList<myCluster> cluster = new ArrayList<>();
    double delta = 0;
    do{
        Set<Double,Cluster> distanzen = new TreeMap<>();

    }while(delta > 0);

}